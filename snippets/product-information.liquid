{%- assign side_panels = false -%}
<div id="ProductInfo-{{ section.id }}" class="product-information--inner">
  {%- assign product_form_id = 'product-form-' | append: section.id -%}
  {%- assign outline_button = false -%}
  {%- for block in section.blocks -%}
    {%- case block.type -%}
    {%- when '@app' -%}
      {% render block %}
    {%- when 'breadcrumbs' -%}
      {%- comment -%} Same trail/logic as the standalone Breadcrumbs section (snippets/breadcrumbs.liquid), just placeable inside Product information as a block. {%- endcomment -%}
      <div class="product-information--breadcrumbs" {{ block.shopify_attributes }}>
        {%- render 'breadcrumbs' -%}
      </div>
    {%- when 'label' -%}
      {%- render 'product-card-labels', product: product -%}
    {%- when 'text' -%}
      <p class="product--text {{ block.settings.text_style }}" {{ block.shopify_attributes }}>
        {{- block.settings.text -}}
      </p>
    {%- when 'custom_liquid' -%}
      {{ block.settings.custom_liquid }}
    {%- when 'title' -%}
      {%- liquid
        assign title_size_d = block.settings.title_font_size_desktop
        assign title_size_m = block.settings.title_font_size_mobile
        assign title_font_d = block.settings.title_font_desktop
        assign title_font_m = block.settings.title_font_mobile
        assign title_font_picker_d = block.settings.title_font_desktop_picker
        assign title_font_picker_m = block.settings.title_font_mobile_picker
        assign title_case_d = block.settings.title_case_desktop
        assign title_case_m = block.settings.title_case_mobile
        assign title_margin_top_d = block.settings.title_margin_top_desktop
        assign title_margin_bottom_d = block.settings.title_margin_bottom_desktop
        assign title_margin_top_m = block.settings.title_margin_top_mobile
        assign title_margin_bottom_m = block.settings.title_margin_bottom_mobile
      -%}
      {%- capture title_classes -%}
        {%- if title_size_d > 0 %} product-title--size-desktop{% endif -%}
        {%- if title_size_m > 0 %} product-title--size-mobile{% endif -%}
        {%- if title_font_d == 'heading' or title_font_d == 'body' or title_font_d == 'custom' %} product-title--font-desktop{% endif -%}
        {%- if title_font_m == 'heading' or title_font_m == 'body' or title_font_m == 'custom' %} product-title--font-mobile{% endif -%}
        {%- if title_case_d == 'uppercase' %} product-title--case-upper-desktop{% elsif title_case_d == 'lowercase' %} product-title--case-lower-desktop{% elsif title_case_d == 'capitalize' %} product-title--case-capitalize-desktop{% elsif title_case_d == 'none' %} product-title--case-normal-desktop{% endif -%}
        {%- if title_case_m == 'uppercase' %} product-title--case-upper-mobile{% elsif title_case_m == 'lowercase' %} product-title--case-lower-mobile{% elsif title_case_m == 'capitalize' %} product-title--case-capitalize-mobile{% elsif title_case_m == 'none' %} product-title--case-normal-mobile{% endif -%}
      {%- endcapture -%}
      {%- capture title_style -%}
        {%- if title_size_d > 0 -%}--product-title-size-desktop: {{ title_size_d }}px;{%- endif -%}
        {%- if title_size_m > 0 -%}--product-title-size-mobile: {{ title_size_m }}px;{%- endif -%}
        {%- if title_font_d == 'heading' -%}--product-title-font-desktop: {{ settings.heading_font.family }}, {{ settings.heading_font.fallback_families }};{%- elsif title_font_d == 'body' -%}--product-title-font-desktop: {{ settings.body_font.family }}, {{ settings.body_font.fallback_families }};{%- elsif title_font_d == 'custom' and title_font_picker_d -%}--product-title-font-desktop: {{ title_font_picker_d.family }}, {{ title_font_picker_d.fallback_families }};{%- endif -%}
        {%- if title_font_m == 'heading' -%}--product-title-font-mobile: {{ settings.heading_font.family }}, {{ settings.heading_font.fallback_families }};{%- elsif title_font_m == 'body' -%}--product-title-font-mobile: {{ settings.body_font.family }}, {{ settings.body_font.fallback_families }};{%- elsif title_font_m == 'custom' and title_font_picker_m -%}--product-title-font-mobile: {{ title_font_picker_m.family }}, {{ title_font_picker_m.fallback_families }};{%- endif -%}
      {%- endcapture -%}
      {%- capture title_container_classes -%}
        {%- if title_margin_top_d > 0 %} product-title-container--margin-top-desktop{% endif -%}
        {%- if title_margin_bottom_d > 0 %} product-title-container--margin-bottom-desktop{% endif -%}
        {%- if title_margin_top_m > 0 %} product-title-container--margin-top-mobile{% endif -%}
        {%- if title_margin_bottom_m > 0 %} product-title-container--margin-bottom-mobile{% endif -%}
      {%- endcapture -%}
      {%- capture title_container_style -%}
        {%- if title_margin_top_d > 0 -%}--product-title-margin-top-desktop: {{ title_margin_top_d }}px;{%- endif -%}
        {%- if title_margin_bottom_d > 0 -%}--product-title-margin-bottom-desktop: {{ title_margin_bottom_d }}px;{%- endif -%}
        {%- if title_margin_top_m > 0 -%}--product-title-margin-top-mobile: {{ title_margin_top_m }}px;{%- endif -%}
        {%- if title_margin_bottom_m > 0 -%}--product-title-margin-bottom-mobile: {{ title_margin_bottom_m }}px;{%- endif -%}
      {%- endcapture -%}
      {%- if title_font_d == 'custom' and title_font_picker_d -%}
        <style>{{ title_font_picker_d | font_face: font_display: 'swap' }}</style>
      {%- endif -%}
      {%- if title_font_m == 'custom' and title_font_picker_m -%}
        <style>{{ title_font_picker_m | font_face: font_display: 'swap' }}</style>
      {%- endif -%}
      <div class="product-title-container{{ title_container_classes }}"{% if title_container_style != blank %} style="{{ title_container_style | strip_newlines | strip }}"{% endif %} {{ block.shopify_attributes }}>
        {%- assign title_placeholder = 'onboarding.product_title' | t -%}
        <h1 class="product-title uppercase--{{ block.settings.uppercase }}{{ title_classes }}"{% if title_style != blank %} style="{{ title_style | strip_newlines | strip }}"{% endif %}>{{ product.title | default: title_placeholder }}</h1>
        <div class="product-price-container">
          <div class="no-js-hidden" id="price-{{ section.id }}">
            {% render 'product-price', product: product, use_variant: true, show_badges: true, show_custom_badges: block.settings.show_custom_badges %}
            {% render 'product-color-swatches', product: product %}
          </div>
          {%- if section.settings.show_taxes_included != blank -%}
            {%- if cart.taxes_included or shop.shipping_policy.body != blank -%}
              <div class="product__tax caption">
                {%- if cart.taxes_included -%}
                  {{ 'products.product.include_taxes' | t }}
                {%- endif -%}
                {%- if shop.shipping_policy.body != blank -%}
                  {{ 'products.product.shipping_policy_html' | t: link: shop.shipping_policy.url }}
                {%- endif -%}
              </div>
            {%- endif -%}
          {%- endif -%}
        </div>
        {%- if block.settings.show_products_rating and product.metafields.reviews.rating.value != blank -%}
          <div class="star-rating-container">
            <div class="star-rating" style="--star-rating: {{ product.metafields.reviews.rating.value.rating }};"></div>
            <span>{{ 'products.product.reviews' | t: count: product.metafields.reviews.rating_count }}</span>
          </div>
        {%- endif -%}
        {%- if product != blank -%}
          {%- form 'product', product, id: 'product-form-installment', class: 'installment' -%}
            <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
            {{ form | payment_terms }}
          {%- endform -%}
        {%- endif -%}
      </div>
    {%- when 'description' -%}
      <div class="product-short-description rte" {{ block.shopify_attributes }}>{{ product.description }}</div>
    {%- when 'inventory_level' -%}
      {% render 'product-inventory-notice', product: product, section: section, block: block %}
    {%- when 'variant_picker' -%}
      {%- unless product.has_only_default_variant -%}
      {%- liquid
        assign is_disabled = block.settings.variant_is_disabled
        assign picker_type = 'dropdown'
	      assign color_picker = true
        assign color_picker_size = block.settings.color_picker_size | default: 'regular'
        assign color_picker_use_variants = block.settings.color_picker_use_variants
      -%}
      <variant-selects
        id="variant-selects-{{ section.id }}"
        class="no-js-hidden"
        data-update-url="{{ update_url }}"
        data-section="{{ section.id }}"
        data-url="{{ product.url }}"
        data-is-disabled="{{ is_disabled }}"
        data-hide-variants="{{ hide_variants }}"
      >
        <div class="variations" {{ block.shopify_attributes }}>
          {%- for option in product.options_with_values -%}
            {%- liquid
              assign option_name = option.name | downcase | escape
              assign option_type = block.settings.picker_type
              assign picker_type = option_type
              assign swatch_count = option.values | map: 'swatch' | compact | size 
              if block.settings.color_picker
                if option_name contains 'color' or option_name contains 'colour' or option_name contains 'couleur' or option_name contains 'farbe'
                  assign option_type = 'color'
                endif
              endif

              # Additional swatch variant options set inside theme settings
              assign color_swatches_variant_option = settings.color_swatches_variant_option | downcase | split: ', '

              if color_swatches_variant_option contains option_name
                assign option_type = 'color'
              endif

              unless block.settings.color_picker
                assign color_picker = false
              endunless

              if swatch_count > 0
                assign option_type = 'color'
              endif
            -%}
            {%- liquid
              assign option_slug = option.name | downcase
              assign sizing_link = ''

              assign sizing_guide_variant = block.settings.sizing_guide_variant | default: 'Size' | downcase
            -%}
            {%- if option_slug == sizing_guide_variant -%}
              {%- if block.settings.sizing_guide_text != empty -%}
                  {%- capture sizing_link -%}
                    <modal-opener class="product-popup-modal__opener no-js-hidden" data-modal="#PopupModal-{{ block.id }}">
                      <button id="ProductPopup-{{ block.id }}" class="product-popup-modal__button" type="button" aria-haspopup="dialog">
                        {% render 'svg-icons-inline', icon: block.settings.sizing_guide_icon %}<span>{{ block.settings.sizing_guide_text }}</span></button>
                    </modal-opener>
                  {%- endcapture -%}
                  {%- capture sizing_popup -%}
                  <modal-dialog id="PopupModal-{{ block.id }}" class="product-popup-modal">
                    <div role="dialog" aria-label="{{ block.settings.sizing_guide_text }}" aria-modal="true" class="product-popup-modal__content" tabindex="-1">
                      <div class="product-popup-modal__content-header">
                        <div class="product-popup-modal__content-header--inner">
                          <span class="h5 body-font">{{ block.settings.sizing_guide_page.title | default: block.settings.sizing_guide_text }}</span>
                          <button id="ModalClose-{{ block.id }}" type="button" class="product-popup-modal__toggle" aria-label="{{ 'accessibility.close' | t }}">{% render 'svg-icons' with 'close' %}</button>
                        </div>
                      </div>
                      <scroll-shadow>
                        <div class="product-popup-modal__content-info">
                          {{ block.settings.sizing_guide_page.content }}
                        </div>
                      </scroll-shadow>
                    </div>
                  </modal-dialog>
                  {%- endcapture -%}
              {%- endif -%}
            {%- endif -%}
            {% render 'product-option', option_type: option_type, color_picker_size: color_picker_size, color_picker_use_variants: color_picker_use_variants, product: product, option: option, forloop: forloop, product_form_id: product_form_id, sizing_link: sizing_link %}
          {%- endfor -%}
        </div>
        <span
          id="SelectedVariant-{{ section.id }}"
          data-variant-id="{{ product.selected_or_first_available_variant.id }}"
          data-available="{{ product.selected_or_first_available_variant.available }}"
          data-featured-media-id="{{ product.selected_or_first_available_variant.featured_media.id }}"
          hidden
        ></span>
      </variant-selects>
      {%- endunless -%}
      <noscript class="product-form__noscript-wrapper-{{ section.id }}">
        <div class="product-form__input product-form__input--dropdown{% if product.has_only_default_variant %} hidden{% endif %}">
          <label for="Variants-{{ section.id }}">{{ 'products.product.product_variants' | t }}</label>
          <div class="select">
            <select name="id" id="Variants-{{ section.id }}" class="select__select full" form="{{ product_form_id }}">
              {%- for variant in product.variants -%}
                <option
                  {% if variant == product.selected_or_first_available_variant %}selected="selected"{% endif %}
                  {% if variant.available == false %}disabled{% endif %}
                  value="{{ variant.id }}">
                  {{ variant.title }}
                  {%- if variant.available == false %} - {{ 'products.product.sold_out' | t }}{% endif %}
                  - {{ variant.price | money | strip_html -}}
                </option>
              {%- endfor -%}
            </select>
            <div class="select-arrow">{%- render 'svg-icons' with 'thb-select' -%}</div>
          </div>
        </div>
      </noscript>
    {%- when 'buy_buttons' -%}
        {%- assign outline_button = block.settings.outline_button -%}
        {% render 'product-add-to-cart', product: product, form: form, product_form_id: product_form_id, block: block, pickup: pickup %}
    {%- when 'share' -%}
        {% render 'social-share', share_label: true, share_title: product.title, share_permalink: product.url, share_image: product.featured_image, block: block %}
    {%- when 'side_panel' -%}
      {%- unless side_panels -%}
      {%- assign side_panels = section.blocks | where: "type", "side_panel" -%}
      <side-panel-links class="side-panel-links">
        {%- for panel in side_panels -%}
          {%- if panel.settings.side_panel_label != '' -%}
          <button class="side-panel-links--link no-js-hidden" data-id="#side-panel-{{ section.id }}-information-panel-{{ forloop.index }}">{% render 'svg-icons-inline', icon: panel.settings.side_panel_icon %}<span class="text-button">{{ panel.settings.side_panel_label }}</span></button>
          {%- endif -%}
        {%- endfor -%}
      </side-panel-links>
      <script src="{{ 'side-panel-links.js' | asset_url }}" defer="defer"></script>
      {%- endunless -%}
    {%- when 'collapsible_tab' -%}
      {%- if block.settings.heading != blank -%}
      {%- liquid
        assign tab_heading_size_d = block.settings.heading_font_size_desktop
        assign tab_heading_size_m = block.settings.heading_font_size_mobile
        assign tab_heading_case = block.settings.heading_case
      -%}
      {%- capture tab_heading_classes -%}
        {%- if tab_heading_size_d > 0 %} product__accordion--size-desktop{% endif -%}
        {%- if tab_heading_size_m > 0 %} product__accordion--size-mobile{% endif -%}
        {%- if tab_heading_case == 'uppercase' %} product__accordion--case-upper{% elsif tab_heading_case == 'lowercase' %} product__accordion--case-lower{% elsif tab_heading_case == 'capitalize' %} product__accordion--case-capitalize{% elsif tab_heading_case == 'none' %} product__accordion--case-normal{% endif -%}
      {%- endcapture -%}
      {%- capture tab_heading_style -%}
        {%- if tab_heading_size_d > 0 -%}--accordion-heading-size-desktop: {{ tab_heading_size_d }}px;{%- endif -%}
        {%- if tab_heading_size_m > 0 -%}--accordion-heading-size-mobile: {{ tab_heading_size_m }}px;{%- endif -%}
      {%- endcapture -%}
      <collapsible-row class="product__accordion accordion{{ tab_heading_classes }}"{% if tab_heading_style != blank %} style="{{ tab_heading_style | strip_newlines | strip }}"{% endif %} {{ block.shopify_attributes }}>
        <details id="Details-{{ block.id }}-{{ section.id }}" {% if block.settings.open %}open{% endif %}>
          <summary>
            {{ block.settings.heading | default: block.settings.page.title }}
            <span></span>
          </summary>
          <div class="collapsible__content accordion__content rte" id="ProductAccordion-{{ block.id }}-{{ section.id }}">
            {{ block.settings.content }}
            {{ block.settings.page.content }}
            {{ block.settings.custom_liquid }}
          </div>
        </details>
      </collapsible-row>
      {%- endif -%}
    {%- when 'complementary' -%}
      {{- 'product-grid.css' | asset_url | stylesheet_tag -}}
      {%- liquid
        assign cp_heading = block.settings.block_heading
        assign cp_heading_color = block.settings.heading_color
        assign cp_heading_size = block.settings.heading_font_size
        assign cp_heading_case = block.settings.heading_case
        assign cp_heading_font = block.settings.heading_font
        assign cp_heading_font_picker = block.settings.heading_font_picker
        assign cp_margin_top_d = block.settings.margin_top_desktop
        assign cp_margin_bottom_d = block.settings.margin_bottom_desktop
        assign cp_margin_top_m = block.settings.margin_top_mobile
        assign cp_margin_bottom_m = block.settings.margin_bottom_mobile
        assign cp_gap_desktop = block.settings.carousel_gap_desktop
        assign cp_gap_mobile = block.settings.carousel_gap_mobile
      -%}
      {%- capture cp_heading_classes -%}
        {%- if cp_heading_case == 'uppercase' %} complementary-products--heading-case-upper{% elsif cp_heading_case == 'lowercase' %} complementary-products--heading-case-lower{% elsif cp_heading_case == 'capitalize' %} complementary-products--heading-case-capitalize{% elsif cp_heading_case == 'none' %} complementary-products--heading-case-normal{% endif -%}
      {%- endcapture -%}
      {%- capture cp_heading_style -%}
        {%- if cp_heading_size > 0 -%}font-size: {{ cp_heading_size }}px;{%- endif -%}
        {%- if cp_heading_color != blank -%}color: {{ cp_heading_color }};{%- endif -%}
        {%- if cp_heading_font == 'heading' -%}font-family: {{ settings.heading_font.family }}, {{ settings.heading_font.fallback_families }};{%- elsif cp_heading_font == 'body' -%}font-family: {{ settings.body_font.family }}, {{ settings.body_font.fallback_families }};{%- elsif cp_heading_font == 'custom' and cp_heading_font_picker -%}font-family: {{ cp_heading_font_picker.family }}, {{ cp_heading_font_picker.fallback_families }};{%- endif -%}
      {%- endcapture -%}
      {%- capture cp_section_style -%}
        {%- if cp_margin_top_d != blank -%}--cp-margin-top-desktop: {{ cp_margin_top_d }}px;{%- endif -%}
        {%- if cp_margin_bottom_d != blank -%}--cp-margin-bottom-desktop: {{ cp_margin_bottom_d }}px;{%- endif -%}
        {%- if cp_margin_top_m != blank -%}--cp-margin-top-mobile: {{ cp_margin_top_m }}px;{%- endif -%}
        {%- if cp_margin_bottom_m != blank -%}--cp-margin-bottom-mobile: {{ cp_margin_bottom_m }}px;{%- endif -%}
        {%- if cp_gap_desktop != blank -%}--cp-carousel-gap-desktop: {{ cp_gap_desktop }}px;{%- endif -%}
        {%- if cp_gap_mobile != blank -%}--cp-carousel-gap-mobile: {{ cp_gap_mobile }}px;{%- endif -%}
      {%- endcapture -%}
      {%- if cp_heading_font == 'custom' and cp_heading_font_picker -%}
        <style>{{ cp_heading_font_picker | font_face: font_display: 'swap' }}</style>
      {%- endif -%}
      <product-recommendations class="product-recommendations complementary-products no-js-hidden" data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ product.id }}&limit={{ block.settings.product_list_limit }}&intent=complementary"{% if cp_section_style != blank %} style="{{ cp_section_style | strip_newlines | strip }}"{% endif %} {{ block.shopify_attributes }}>
        {%- if recommendations.performed and recommendations.products_count > 0 -%}
          {%- assign cp_is_carousel = false -%}
          {%- if recommendations.products_count > 3 -%}
            {%- assign cp_is_carousel = true -%}
          {%- endif -%}
          <div class="complementary-products--header">
            {%- if cp_heading != blank -%}
              <div class="complementary-products--heading{{ cp_heading_classes }}"{% if cp_heading_style != blank %} style="{{ cp_heading_style | strip_newlines | strip }}"{% endif %}>{{ cp_heading }}</div>
            {%- endif -%}
            {%- if cp_is_carousel -%}
              <div class="product-recommendations--pager complementary-products--pager">
                <div class="flickity-nav flickity-prev" tabindex="0">{%- render 'svg-icons' with 'carousel-prev' -%}</div>
                <span class="product-recommendations--pager-count"><span class="product-recommendations--pager-current">1</span> / <span class="product-recommendations--pager-total">1</span></span>
                <div class="flickity-nav flickity-next" tabindex="0">{%- render 'svg-icons' with 'carousel-next' -%}</div>
              </div>
            {%- endif -%}
          </div>
          {%- if cp_is_carousel -%}
            <slide-show class="complementary-products--carousel carousel--paged products carousel" data-align="left">
              {%- for recommendation in recommendations.products -%}
                <div class="carousel__slide">
                  {% render 'product-card', product_card_product: recommendation %}
                </div>
              {%- endfor -%}
            </slide-show>
          {%- else -%}
            <ul class="products collection row small-up-2 medium-up-3 no-padding complementary-products--grid">
              {%- for recommendation in recommendations.products -%}
                <li class="columns">
                  {% render 'product-card', product_card_product: recommendation %}
                </li>
              {%- endfor -%}
            </ul>
          {%- endif -%}
        {%- endif -%}
      </product-recommendations>
    {%- when 'image' -%}
      <div class="product-information--image {{ block.settings.position }}" {{ block.shopify_attributes }}>
        {%- if block.settings.image_link -%}<a href="{{ block.settings.image_link }}">{%- endif -%}
        {% if block.settings.image %}
        {%- liquid
          assign image = block.settings.image
          assign sizes = image.src.width | append: 'x' | append: image.src.height
          render 'responsive-image', image: image, sizes: sizes
          -%}
        {%- else -%}
          <div class="thb-placeholder">
            {{ 'image' | placeholder_svg_tag: 'placeholder_svg_tag' }}
          </div>
        {%- endif -%}
        {%- if block.settings.image_link -%}</a>{%- endif -%}
      </div>
    {% when 'custom_option' %}
      {%- if block.settings.label != blank -%}
        {%- liquid
          assign line_item_input_id = product_form_id | append: '-custom-' | append: forloop.index
          assign line_item_input_name = block.settings.label | escape | prepend: 'properties[' | append: ']'
          assign required = block.settings.text_required
          assign text_placeholder = block.settings.text_placeholder
        -%}
        <fieldset class="product-form__input product-information--line-item" {{ block.shopify_attributes}}>
          <div class="form__label" for="{{ line_item_input_id }}">{{ block.settings.label | escape }}</div>
          {% case block.settings.type %}
            {% when 'text' %}
                <input class="product-information--line-item--text" type="text" id="{{ line_item_input_id }}" form="{{ product_form_id }}" name="{{ line_item_input_name }}" placeholder="{{ text_placeholder }}" form="{{ product_form_id }}" {% if required %}required{% endif %}>

            {% when 'textarea' %}
                <textarea class="product-information--line-item--textarea" id="{{ line_item_input_id }}" form="{{ product_form_id }}" name="{{ line_item_input_name }}" rows="3" placeholder="{{ text_placeholder }}" form="{{ product_form_id }}" {% if required %}required{% endif %}></textarea>

            {% when 'checkbox' %}
                <input type="hidden" form="{{ product_form_id }}" name="{{ line_item_input_name }}" value="{{ block.settings.checkbox_unchecked_value | escape }}">
                <input type="checkbox" class="custom-checkbox" form="{{ product_form_id }}" name="{{ line_item_input_name }}" value="{{ block.settings.checkbox_checked_value | escape }}" id="{{ line_item_input_id }}" {% if required %}required{% endif %}>

            {% when 'multiple' %}
              {%- assign custom_options = block.settings.multiple_options | newline_to_br | strip_newlines | split: '<br />' -%}

              {% if block.settings.multiple_style == 'dropdown' %}
                <div class="select">
                  <select name="{{ line_item_input_name }}" id="{{ line_item_input_id }}" form="{{ product_form_id }}">
                    {% for option in custom_options %}
                      <option value="{{ option | escape }}" {% if forloop.first %} selected{% endif %}>{{ option | escape }}</option>
                    {% endfor %}
                  </select>
                  <div class="select-arrow">{%- render 'svg-icons' with 'thb-select' -%}</div>
                </div>
              {% else %}
                {% for option in custom_options %}
                  <label class="label">
                    <input type="radio" form="{{ product_form_id }}" name="{{ line_item_input_name }}" value="{{ option | escape }}"{% if forloop.first %} checked{% endif %}>
                    <span class="label__text-beside-input">{{ option | escape }}</span>
                  </label>
                {% endfor %}
              {% endif %}
          {% endcase %}
        </fieldset>
      {%- endif -%}
    {%- when 'sku' -%}
      <div class="product-sku product--text {{ block.settings.sku_style }}" id="sku-{{ section.id }}" {{ block.shopify_attributes }}>{{ product.selected_or_first_available_variant.sku }}</div>
    {%- when 'product_link' -%}
			<a href="{{ product.url }}" title="{{ 'products.product.view_product_details' | t | escape }}" class="text-button view-product-details" {{ block.shopify_attributes }}>{{ 'products.product.view_product_details' | t }}</a>
    {%- when 'border' -%}
			<hr class="product-information--line" />
    {%- when 'siblings' -%}
      {%- render 'product-siblings' product: product, product_form_id: product_form_id, block: block -%}
    {%- when 'shipping_estimator' -%}
      <collapsible-row class="product__accordion accordion" {{ block.shopify_attributes }}>
        <details id="Details-{{ block.id }}-{{ section.id }}" {% if block.settings.open %}open{% endif %}>
          <summary>
            {{- 'sections.cart.shipping_estimator.estimate_shipping' | t -}}
            <span>{%- render 'svg-icons' with 'thb-collapsible-arrow' -%}</span>
          </summary>
          <div class="collapsible__content accordion__content rte" id="ProductAccordion-{{ block.id }}-{{ section.id }}">
            {%- render 'shipping-estimator', section: section, shipping_estimator: block -%}
          </div>
        </details>
      </collapsible-row>
    {%- when 'fit_guide' -%}
      <div class="product-fit-guide" style="--active-color: {{ block.settings.color_active_segment }};" {{ block.shopify_attributes }}>
        {%- liquid
          assign block_heading = block.settings.block_heading
          assign label_active = block.settings.label_active | abs | at_most: 5 | at_least: 1
          assign label_left = block.settings.label_left
          assign label_middle = block.settings.label_middle
          assign label_right = block.settings.label_right
        -%}
        {%- if block_heading != blank -%}<div class="product-fit-guide--heading">{{ block_heading }}</div>{%- endif -%}
        <div class="product-fit-guide--lines">
          {%- for i in (1..5) -%}
          <div class="product-fit-guide--line{% if label_active == i %} product-fit-guide--line--active{%- endif -%}"></div>
          {%- endfor -%}
        </div>
        <div class="product-fit-guide--labels">
          <div class="product-fit-guide--label-left">{{ label_left }}</div>
          {%- if label_middle != blank -%}<div class="product-fit-guide--label-middle">{{ label_middle }}</div>{%- endif -%}
          <div class="product-fit-guide--label-right">{{ label_right }}</div>
        </div>
        <link rel="stylesheet" href="{{ 'product-fit.css' | asset_url }}" media="print" onload="this.media='all'">
      </div>
  {%- endcase -%}
  {% endfor %}
</div>
{%- if section.settings.enable_sticky_add_to_cart -%}
{% render 'product-add-to-cart-sticky', product: product, media: first_image, color_picker: color_picker, picker_type: picker_type, form: form, is_disabled: is_disabled, outline_button: outline_button %}
{%- endif -%}
{{ sizing_popup }}